# Copy to grafana-secret.yaml, replace the password, and apply it. The real
# secret is gitignored.
apiVersion: v1
kind: Secret
metadata:
  name: grafana-secret
  namespace: dental-clinic
type: Opaque
stringData:
  GF_SECURITY_ADMIN_USER: admin
  GF_SECURITY_ADMIN_PASSWORD: change-me
